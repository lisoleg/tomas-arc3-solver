# === Core numerical computation ===
numpy>=1.26.0
scipy>=1.12.0
networkx>=3.2

# === Deep learning (GPU verification) ===
torch>=2.1.0
torchvision>=0.16.0

# === Configuration & serialization ===
pyyaml>=6.0

# === HTTP client (DeepSeek API) ===
httpx>=0.27.0

# === Logging & monitoring ===
loguru>=0.7.0
tqdm>=4.66.0
tensorboard>=2.16.0

# === Testing ===
pytest>=8.0.0
pytest-cov>=4.1.0

# === Code quality ===
ruff>=0.4.0
mypy>=1.10.0

# === Numba JIT compilation (v2.2) ===
numba>=0.59.0

# === GPU acceleration (v2.3, optional) ===
# Install one of the following based on your CUDA version:
# cupy-cuda12x>=13.0  # for CUDA 12.x (Kaggle T4)
# cupy-cuda11x>=13.0  # for CUDA 11.x
# When cupy is not installed, the solver automatically falls back
# to CPU (Numba/NumPy) with no functionality loss.
